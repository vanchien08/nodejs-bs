<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- <%= JSON.stringify(userEdit) %> -->
    <div class="topnav">
        <a  href="/home">Home</a>
        <a   href="/create" >Create</a>
        <a class="active" href="/update">update</a>
        <a href="#about">About</a>
      </div>
      <div class="form">
        <form action="/update-user" method ="post">

        
        <fieldset>
            <legend>
                Update
            </legend>
            <div class="input id_update_disable" >
                <lable>Email</lable>
                <input type="text" value="<%= userEdit.id %>" id="emailid" name="id" />
            </div>
            <div class="input" >
                <lable>Email</lable>
                <input type="text" value="<%= userEdit.email %>" id="emailid" name="email" />
            </div>
            <div class="input" >
                <lable>Name</lable>
                <input type="'text" value="<%= userEdit.name %>" id="nameid" name="name"/>
            </div>
            <div class="input" >
                <lable>City</lable>
                <input type="'text" value="<%= userEdit.city %>" id="Cityid" name="City"/>
            </div>
            <div class="button">
                <button>
                    save
                </button>
            </div>
        </fieldset>

    </form>
      </div>
</body>
</html>